/*
  Experience section removed per user request.
  Original content is kept here as a comment for reference.

  (The Experience component previously rendered a timeline using the
  `experiences` data from `src/constants.js`.)
*/

import React from "react";

const Experience = () => {
  // No experience to show — return null so the component renders nothing.
  return null;
};

export default Experience;
